{ config, pkgs, inputs, ... }:

{
  imports = [
    inputs.xremap-flake.nixosModules.default
  ];

  services.xremap = {
    userName = "matthew";
    config.modmap = [
      {
        name = "Global";
        remap = {"CapsLock" = "Esc"; };        
      }
    ];
  };
  
}
